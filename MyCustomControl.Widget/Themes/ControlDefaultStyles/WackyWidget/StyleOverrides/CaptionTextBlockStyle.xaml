<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="clr-namespace:MyCustomControl.Widget"
                    xmlns:themes="clr-namespace:MyCustomControl.ThemeController;assembly=MyCustomControl.ThemeController">
  <ResourceDictionary.MergedDictionaries>

    <!-- Provide the default/fallback theme -->
    <ResourceDictionary Source="/MyCustomControl.Widget;component/Themes/Themes/LightThemes/Application.Light.ThemeResources.xaml" />
  </ResourceDictionary.MergedDictionaries>
  <Style TargetType="TextBlock"
         x:Shared="False"
         x:Key="{x:Static local:WackyWidget.CaptionStyleKey}">

    <!-- Important! -->
    <Setter Property="OverridesDefaultStyle"
            Value="True" />

    <Setter Property="Foreground"
            Value="{DynamicResource {x:Static themes:ResourceKeys.CaptionForegroundBrushKey}}" />
    <Setter Property="HorizontalAlignment"
            Value="Stretch" />
    <Setter Property="Margin"
            Value="2" />
    <Setter Property="FontWeight"
            Value="Bold" />
  </Style>
</ResourceDictionary>
